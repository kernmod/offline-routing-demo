# Live API proof — 2026-08-31

- Origin: `https://offline-routing-segments.yaktrak.workers.dev`
- D1: `offline-routing-segments`, region OC
- Worker version: `039bcf9f-0137-491e-bd5a-1977c3ca0639`
- Migrations: `0001_init.sql` and `0002_seed_segments.sql` applied remotely

The production verifier was run from a separate client process:

```bash
pnpm verify:live-api --url https://offline-routing-segments.yaktrak.workers.dev
```

It checks D1 health, publishes one fixed and bounded three-point Sydney geometry,
then reads the exact returned record through `GET /segments?bbox=…`.

```text
LIVE_API_OK health=200 publish=201 nearby=200
```

The verifier emits neither the random idempotency key nor request/response bodies.
Cloudflare credentials and the rate-limit salt remain outside the repository.

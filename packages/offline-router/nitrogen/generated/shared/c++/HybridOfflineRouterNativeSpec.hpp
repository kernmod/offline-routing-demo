///
/// HybridOfflineRouterNativeSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2026 Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `Coordinate` to properly resolve imports.
namespace margelo::nitro::offlinerouter { struct Coordinate; }

#include <string>
#include <NitroModules/ArrayBuffer.hpp>
#include "Coordinate.hpp"
#include <vector>

namespace margelo::nitro::offlinerouter {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `OfflineRouterNative`
   * Inherit this class to create instances of `HybridOfflineRouterNativeSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridOfflineRouterNative: public HybridOfflineRouterNativeSpec {
   * public:
   *   HybridOfflineRouterNative(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridOfflineRouterNativeSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridOfflineRouterNativeSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridOfflineRouterNativeSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string loadPack(const std::shared_ptr<ArrayBuffer>& pack) = 0;
      virtual std::string route(const Coordinate& origin, const Coordinate& destination) = 0;
      virtual std::string routeMany(const std::vector<Coordinate>& controls, bool closedLoop) = 0;
      virtual std::string benchmark(const std::string& device) = 0;
      virtual double startTileServer(const std::string& assetDirectory, double port) = 0;
      virtual void stopTileServer() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "OfflineRouterNative";
  };

} // namespace margelo::nitro::offlinerouter
